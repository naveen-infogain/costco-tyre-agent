fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# LangChain + LangGraph (agent framework)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0          # create_react_agent + MemorySaver (replaces AgentExecutor in LangChain 1.x)

# Anthropic Claude API
anthropic>=0.34.0

# Data validation
pydantic>=2.9.0

# Environment + server
python-dotenv>=1.0.1
python-multipart>=0.0.9   # multipart form support for FastAPI
aiofiles>=23.2.1           # async static file serving

# HTTP client (ElevenLabs TTS streaming)
httpx>=0.27.0

# Calendar event generation (.ics files)
icalendar>=5.0.12

# ElevenLabs voice (optional — TTS disabled if ELEVENLABS_API_KEY not set)
elevenlabs>=1.3.0
